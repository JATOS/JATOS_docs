spec:
  name: jatos-test
  services:
  - image:
      registry_type: DOCKER_HUB
      registry: jatos
      repository: jatos
      tag: latest
    name: jatos-test
    http_port: 9000
    instance_count: 1
