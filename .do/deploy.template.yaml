spec:
  name: jatos
  services:
  - image:
      registry_type: DOCKER_HUB
      registry: jatos
      repository: jatos
      tag: latest
    name: jatos
    http_port: 9000
    routes:
    - path: /
    envs:
    - key: JATOS_LOGS_APPENDER
      value: ASYNCSTDOUT
